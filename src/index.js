import React from 'react';
import ReactDOM from 'react-dom';
import BookingCheckPage from './components/BookingCheckPage'


//使用ReactDOM.render把剛建立的物件element插入目標DOM中
ReactDOM.render(
  <BookingCheckPage/>,
  document.getElementById('root')
);